module.exports = [
    { name: 'tomp3', aliases: ['mp3', 'audio'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎵 Convertiendo archivo a MP3...' }); } },
    { name: 'bass', aliases: ['bassboost'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔊 Agregando bajos profundos al audio...' }); } },
    { name: 'pitch', aliases: ['ardilla', 'chipmunk'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🐿️ Efecto voz de ardilla aplicado...' }); } },
    { name: 'deep', aliases: ['grave', 'demon'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '👹 Efecto voz grave aplicado...' }); } },
    { name: 'slow', aliases: ['lentopitch'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🐢 Audio en cámara lenta (Slowed)...' }); } },
    { name: 'fast', aliases: ['rapidoaudio'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '⚡ Audio acelerado (Sped up)...' }); } },
    { name: 'reverb', aliases: ['eco', 'reverberacion'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🏛️ Efecto de eco/reverberación aplicado...' }); } },
    { name: 'nightcore', aliases: ['nc'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✨ Versión Nightcore generada...' }); } },
    { name: '8d', aliases: ['8daudio'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎧 Efecto de audio envolvente 8D...' }); } },
    { name: 'reverseaudio', aliases: ['audioreversa'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔄 Audio reproducido al revés...' }); } },
    { name: 'cutaudio', aliases: ['cortaraudio'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✂️ Cortando fragmento de audio...' }); } },
    { name: 'joinaudio', aliases: ['uniraudio'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔗 Uniendo pistas de audio...' }); } },
    { name: 'volume', aliases: ['volumen'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔊 Ajustando nivel de volumen...' }); } },
    { name: 'earrape', aliases: ['satan'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '💥 Audio earrape extremo...' }); } },
    { name: 'robot', aliases: ['vozrobot'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🤖 Efecto de voz robótica...' }); } },
    { name: 'phone', aliases: ['telefonillo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📞 Efecto de llamada telefónica...' }); } },
    { name: 'tovn', aliases: ['nota', 'ptt'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎤 Convertido a nota de voz oficial de WhatsApp...' }); } },
    { name: 'toflac', aliases: ['flac'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎵 Convertido a formato FLAC sin pérdida...' }); } },
    { name: 'toogg', aliases: ['ogg'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📦 Convertido a formato OGG...' }); } },
    { name: 'towav', aliases: ['wav'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎧 Convertido a formato WAV...' }); } },
    { name: 'tom4a', aliases: ['m4a'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎶 Convertido a formato M4A...' }); } },
    { name: 'audiomuter', aliases: ['silenciaraudio'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔇 Silenciando audio...' }); } },
    { name: 'audiofadein', aliases: ['afadein'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✨ Fade in aplicado a audio...' }); } },
    { name: 'audiofadeout', aliases: ['afadeout'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✨ Fade out aplicado a audio...' }); } },
    { name: 'audioloop', aliases: ['aloop'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔁 Audio en bucle...' }); } },
    { name: 'equalizer', aliases: ['eq'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎛️ Ecualizador de audio aplicado...' }); } },
    { name: 'audiodenoise', aliases: ['denoise'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🧹 Reducción de ruido ambiental...' }); } },
    { name: 'audiocompress', aliases: ['comprimiraudio'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📦 Comprimiendo peso de audio...' }); } },
    { name: 'audiobitrate', aliases: ['abitrate'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📊 Bitrate de audio ajustado...' }); } },
    { name: 'audiofreq', aliases: ['frecuencia'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📈 Frecuencia de muestreo cambiada...' }); } },
    { name: 'audiosplit', aliases: ['splitpista'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✂️ Separando voz e instrumental...' }); } },
    { name: 'karaoke', aliases: ['instrumental'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎤 Modo karaoke (Voz removida)...' }); } },
    { name: 'acapella', aliases: ['solofrases'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎙️ Extrayendo solo la voz acapella...' }); } },
    { name: 'audionormalize', aliases: ['normalizar'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📊 Normalizando volumen general...' }); } },
    { name: 'audiopan', aliases: ['panoramico'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎧 Panning estéreo aplicado...' }); } },
    { name: 'audiochorus', aliases: ['coro'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✨ Efecto chorus aplicado...' }); } },
    { name: 'audioflanger', aliases: ['flanger'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🌀 Efecto flanger aplicado...' }); } },
    { name: 'audiophaser', aliases: ['phaser'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✨ Efecto phaser aplicado...' }); } },
    { name: 'audiotremolo', aliases: ['tremolo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎵 Efecto trémolo aplicado...' }); } },
    { name: 'audiovibrato', aliases: ['vibrato'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎶 Efecto vibrato aplicado...' }); } },
    { name: 'audiorecord', aliases: ['arecord'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔴 Grabación de audio procesada...' }); } },
    { name: 'audiomerge', aliases: ['amerge'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔗 Audios mezclados...' }); } },
    { name: 'audiorender', aliases: ['arender'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '⚙️ Renderizando audio final...' }); } },
    { name: 'audiostats', aliases: ['astats'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📊 Estadísticas de audio...' }); } },
    { name: 'audiohelp', aliases: ['ahelp'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📖 Guía de creación de audio.' }); } },
    { name: 'audioclear', aliases: ['aclear'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🧹 Caché de audio limpia.' }); } },
    { name: 'audiolock', aliases: ['alock'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔒 Módulo de audio bloqueado.' }); } },
    { name: 'audiounlock', aliases: ['aunlock'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔓 Módulo de audio desbloqueado.' }); } },
    { name: 'audiostatus', aliases: ['astatus'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🟢 Módulo de audio activo.' }); } },
    { name: 'audiobot', aliases: ['abot'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🤖 Sistema de audio automatizado.' }); } }
];
