module.exports = [
    { name: 'sticker', aliases: ['s'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🖼️ Creando sticker...' }); } },
    { name: 'toimg', aliases: ['imagen'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔄 Sticker a imagen...' }); } },
    { name: 'tovideo', aliases: ['mp4sticker'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎬 Sticker animado a video...' }); } },
    { name: 'wm', aliases: ['packname'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✏️ Cambiando marca de agua...' }); } },
    { name: 'qc', aliases: ['quote'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '💬 Creando sticker de texto...' }); } },
    { name: 'take', aliases: ['robar'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🏷️ Robando sticker...' }); } },
    { name: 'ttp', aliases: ['textsticker'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔤 Texto a sticker...' }); } },
    { name: 'attp', aliases: ['textofull'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎨 Texto animado a sticker...' }); } },
    { name: 'circle', aliases: ['redondo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '⭕ Sticker circular...' }); } },
    { name: 'emojimix', aliases: ['mix'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✨ Mezclando emojis en sticker...' }); } },
    { name: 'dado', aliases: ['dice'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎲 Dado en sticker...' }); } },
    { name: 'tourl', aliases: ['telegraph'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔗 Subiendo archivo a enlace...' }); } },
    { name: 'Trigger', aliases: ['gatillo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🚨 Sticker trigger...' }); } },
    { name: 'wasted', aliases: ['gta'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '💀 Sticker wasted...' }); } },
    { name: 'comunista', aliases: ['ussr'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '☭ Sticker comunista...' }); } },
    { name: 'horny', aliases: ['bonk'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔨 Sticker bonk...' }); } },
    { name: 'simp', aliases: ['simpcard'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '💳 Tarjeta simp...' }); } },
    { name: 'jail', aliases: ['carcel'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔒 Sticker en cárcel...' }); } },
    { name: 'blur', aliases: ['difuminar'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🌫️ Difuminando sticker...' }); } },
    { name: 'invert', aliases: ['invertir'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔄 Colores invertidos...' }); } },
    { name: 'tojpg', aliases: ['jpg'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🖼️ Sticker a JPG...' }); } },
    { name: 'tomp3', aliases: ['audios'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎵 Sticker con audio...' }); } },
    { name: 'stickerfilter', aliases: ['sfilter'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎨 Filtro de sticker...' }); } },
    { name: 'stickersepia', aliases: ['sepia'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📜 Estilo sepia aplicado...' }); } },
    { name: 'stickergray', aliases: ['gris'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '⬛ Escala de grises...' }); } },
    { name: 'patrick', aliases: ['patricio'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '⭐ Patricio sticker...' }); } },
    { name: 'drake', aliases: ['drakememe'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📊 Drake meme sticker...' }); } },
    { name: 'itsatrap', aliases: ['trap'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🚨 Es una trampa...' }); } },
    { name: 'stonks', aliases: ['stockmeme'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📈 Stonks sticker...' }); } },
    { name: 'notstonks', aliases: ['notstock'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📉 Not stonks...' }); } },
    { name: 'panties', aliases: ['panty'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✨ Sticker personalizado...' }); } },
    { name: 'pixel', aliases: ['pixelate'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '👾 Pixelando imagen...' }); } },
    { name: 'bass', aliases: ['bassboost'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔊 Audio con bajo...' }); } },
    { name: 'ebomb', aliases: ['boom'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '💥 Efecto explosión...' }); } },
    { name: 'rip', aliases: ['tumba'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🪦 Sticker RIP...' }); } },
    { name: 'fire', aliases: ['fuego'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔥 Sticker en llamas...' }); } },
    { name: 'lgbt', aliases: ['gay'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🏳️‍🌈 Bandera arcoíris...' }); } },
    { name: 'lolice', aliases: ['policia'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🚨 Lolice card...' }); } },
    { name: 'pen', aliases: ['lapiz'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✏️ Efecto dibujo a lápiz...' }); } },
    { name: 'affect', aliases: ['afecta'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📉 No afecta a mi estilo...' }); } },
    { name: 'baguette', aliases: ['pan'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🥖 Sticker pan...' }); } },
    { name: 'comrade', aliases: ['camarada'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '☭ Camarada sticker...' }); } },
    { name: 'dither', aliases: ['difuminado'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎨 Dither aplicado...' }); } },
    { name: 'toavatarpic', aliases: ['perfilsticker'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🖼️ Foto de perfil a sticker...' }); } },
    { name: 'rcard', aliases: ['cardsticker'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎴 Tarjeta coleccionable...' }); } },
    { name: 'stickerpack', aliases: ['spack'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📦 Pack de stickers...' }); } },
    { name: 'packsticker', aliases: ['pack'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📦 Descargando pack...' }); } },
    { name: 'stickersearch', aliases: ['buscars'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔍 Buscando stickers...' }); } },
    { name: 'randomsticker', aliases: ['sra'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎲 Sticker aleatorio...' }); } },
    { name: 'stickeranim', aliases: ['sanm'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎬 Sticker animado pro...' }); } }
];
