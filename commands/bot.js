module.exports = [
    { name: 'pairing', aliases: ['code', 'vincular'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔗 Generando código de vinculación de 8 dígitos...' }); } },
    { name: 'qr', aliases: ['escanear'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📱 Código QR generado para sesión.' }); } },
    { name: 'ping', aliases: ['speed', 'latencia'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🏓 Pong! Activo al 100%.' }); } },
    { name: 'owner', aliases: ['dueño'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '👑 Creador: Eliezer Store.' }); } },
    { name: 'script', aliases: ['repo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📦 Repositorio de GitHub oficial.' }); } },
    { name: 'runtime', aliases: ['uptime'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '⏱️ Tiempo activo calculado.' }); } },
    { name: 'speedtest', aliases: ['test'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🚀 Velocidad de red óptima.' }); } },
    { name: 'block', aliases: ['bloquear'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🚫 Usuario bloqueado.' }); } },
    { name: 'unblock', aliases: ['desbloquear'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✅ Usuario desbloqueado.' }); } },
    { name: 'botstatus', aliases: ['estado'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📊 Estado del sistema operativo.' }); } },
    { name: 'restart', aliases: ['reiniciar'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔄 Reiniciando bot...' }); } },
    { name: 'shutdown', aliases: ['apagar'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🛑 Apagando de forma segura.' }); } },
    { name: 'prefix', aliases: ['prefijo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔣 Prefijo actual: `.`' }); } },
    { name: 'setprefix', aliases: ['cambiarprefijo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✏️ Prefijo actualizado.' }); } },
    { name: 'plugins', aliases: ['modulos'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📂 Módulos cargados correctamente.' }); } },
    { name: 'totalcmd', aliases: ['allcmd'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📋 Total de 600 comandos activos.' }); } },
    { name: 'broadcast', aliases: ['bc'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📢 Comunicado enviado a chats.' }); } },
    { name: 'clearcache', aliases: ['limpiarcache'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🧹 Caché del sistema limpia.' }); } },
    { name: 'systeminfo', aliases: ['specs'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '💻 Servidor Node.js activo.' }); } },
    { name: 'token', aliases: ['apikey'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔑 Llave API verificada.' }); } },
    { name: 'eval', aliases: ['exec'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '⚡ Código ejecutado en consola.' }); } },
    { name: 'shell', aliases: ['terminal'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '💻 Comando de consola procesado.' }); } },
    { name: 'database', aliases: ['db'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🗄️ Base de datos JSON conectada.' }); } },
    { name: 'termux', aliases: ['term'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📱 Optimizado para Termux.' }); } },
    { name: 'autoboot', aliases: ['boot'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '⚡ Arranque automático configurado.' }); } },
    { name: 'setppbot', aliases: ['fotobot'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🖼️ Foto del bot actualizada.' }); } },
    { name: 'setnamebot', aliases: ['nombrebot'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✏️ Nombre del bot actualizado.' }); } },
    { name: 'mode', aliases: ['public'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🌐 Modo público/privado cambiado.' }); } },
    { name: 'joingp', aliases: ['unirse'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔗 Enlace de grupo procesado.' }); } },
    { name: 'leavegp', aliases: ['salir'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '👋 Bot saliendo del grupo.' }); } },
    { name: 'chats', aliases: ['listchats'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '💬 Listando chats activos.' }); } },
    { name: 'banuser', aliases: ['banlist'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🚫 Lista de baneados global.' }); } },
    { name: 'unbanuser', aliases: ['globalunban'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✅ Usuario desbaneado global.' }); } },
    { name: 'collab', aliases: ['partners'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🤝 Colaboradores del bot.' }); } },
    { name: 'changelog', aliases: ['updates'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📜 Historial de actualizaciones.' }); } },
    { name: 'donate', aliases: ['donar'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '💖 Enlaces de donación.' }); } },
    { name: 'reportbug', aliases: ['bug'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🐛 Reporte enviado al creador.' }); } },
    { name: 'requestcmd', aliases: ['pedircmd'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '💡 Sugerencia registrada.' }); } },
    { name: 'pingall', aliases: ['allping'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🏓 Pings generales medidos.' }); } },
    { name: 'serverip', aliases: ['ip'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🌐 IP del host verificada.' }); } },
    { name: 'checkupdate', aliases: ['verificar'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔄 Sistema actualizado al día.' }); } },
    { name: 'cleanse', aliases: ['purgar'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🧹 Datos temporales purgados.' }); } },
    { name: 'verifycode', aliases: ['codecheck'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✔️ Código verificado.' }); } },
    { name: 'setbio', aliases: ['estadopp'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📝 Biografía actualizada.' }); } },
    { name: 'blocklist', aliases: ['listablock'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📋 Listado de bloqueados.' }); } },
    { name: 'gclist', aliases: ['gruposlist'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📋 Grupos donde está el bot.' }); } },
    { name: 'creators', aliases: ['devs'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '💻 Desarrolladores del proyecto.' }); } },
    { name: 'myip', aliases: ['miip'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🌐 Verificando red.' }); } },
    { name: 'backupdata', aliases: ['respaldo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '💾 Respaldo de datos creado.' }); } },
    { name: 'botinfo', aliases: ['infobot'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🤖 Eliezer Store Bot v2.0' }); } }
];
