module.exports = [
    { name: 'kick', aliases: ['ban', 'echar'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '👢 Usuario expulsado.' }); } },
    { name: 'add', aliases: ['agregar'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '➕ Usuario agregado.' }); } },
    { name: 'grupo', aliases: ['group'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '⚙️ Grupo abierto/cerrado.' }); } },
    { name: 'promote', aliases: ['daradmin'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '⭐ Administrador otorgado.' }); } },
    { name: 'demote', aliases: ['quitaradmin'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📉 Admin retirado.' }); } },
    { name: 'tagall', aliases: ['todos'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📢 Todos etiquetados.' }); } },
    { name: 'hidetag', aliases: ['aviso'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔇 Aviso oculto enviado.' }); } },
    { name: 'link', aliases: ['enlace'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔗 Enlace de invitación.' }); } },
    { name: 'revoke', aliases: ['nuevolink'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔄 Enlace restablecido.' }); } },
    { name: 'infogp', aliases: ['groupinfo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: 'ℹ️ Información del grupo.' }); } },
    { name: 'setname', aliases: ['nuevonombre'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✏️ Nombre de grupo cambiado.' }); } },
    { name: 'setdesc', aliases: ['nuevadesc'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📝 Descripción cambiada.' }); } },
    { name: 'setwelcome', aliases: ['bienvenida'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '👋 Bienvenida configurada.' }); } },
    { name: 'setbye', aliases: ['despedida'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🚪 Despedida configurada.' }); } },
    { name: 'antilink', aliases: ['antienlace'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🛡️ Antienlaces activo.' }); } },
    { name: 'antispam', aliases: ['freno'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🛡️ Antispam activo.' }); } },
    { name: 'warn', aliases: ['advertir'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '⚠️ Advertencia aplicada.' }); } },
    { name: 'unwarn', aliases: ['quitarwarn'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✅ Advertencia removida.' }); } },
    { name: 'listwarns', aliases: ['advertencias'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📋 Lista de warns.' }); } },
    { name: 'admins', aliases: ['listadmins'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '👑 Lista de admins.' }); } },
    { name: 'fantasmas', aliases: ['inactivos'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '👻 Buscando fantasmas.' }); } },
    { name: 'poll', aliases: ['encuesta'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📊 Encuesta creada.' }); } },
    { name: 'del', aliases: ['delete', 'borrar'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🗑️ Mensaje eliminado por el bot.' }); } },
    { name: 'reiniciarcontador', aliases: ['resetmemoria'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🧹 Memoria temporal limpiada (Chats de WhatsApp intactos).' }); } },
    { name: 'antibadword', aliases: ['antimalcriados'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🛡️ Antiglishe/groserías activo.' }); } },
    { name: 'antilinktiktok', aliases: ['antitt'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🛡️ Antitiktok activo.' }); } },
    { name: 'antilinkyt', aliases: ['antilyt'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🛡️ Antiyoutube activo.' }); } },
    { name: 'antitoxic', aliases: ['antitoxicos'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🛡️ Antitóxicos activo.' }); } },
    { name: 'infogroup', aliases: ['ginfo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: 'ℹ️ Datos extendidos del grupo.' }); } },
    { name: 'promoteall', aliases: ['daradminall'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '⭐ Admin a todos (Prohibido).' }); } },
    { name: 'demoteall', aliases: ['quitaradminall'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📉 Quitando admins.' }); } },
    { name: 'setppgp', aliases: ['fotogrupo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🖼️ Foto de grupo cambiada.' }); } },
    { name: 'lock', aliases: ['cerrar'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔒 Grupo cerrado (solo admins).' }); } },
    { name: 'unlock', aliases: ['abrir'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔓 Grupo abierto para todos.' }); } },
    { name: 'slogan', aliases: ['lema'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '💬 Slogan del grupo fijado.' }); } },
    { name: 'tag', aliases: ['mencionar'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📢 Mencionado usuario.' }); } },
    { name: 'totag', aliases: ['marcar'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📢 Marcando miembros.' }); } },
    { name: 'kicknum', aliases: ['echarex'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '👢 Expulsando por prefijo de país.' }); } },
    { name: 'linkgc', aliases: ['enlacegp'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔗 Link del chat actual.' }); } },
    { name: 'setexpire', aliases: ['temporizador'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '⏱️ Mensajes temporales ajustados.' }); } },
    { name: 'gctime', aliases: ['tiempo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '⏰ Temporizador de grupo.' }); } },
    { name: 'autoadm', aliases: ['selfadmin'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '⭐ Verificando admin.' }); } },
    { name: 'listbots', aliases: ['botslist'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🤖 Listando bots en el grupo.' }); } },
    { name: 'welcomeon', aliases: ['bienvenidaon'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '👋 Bienvenidas encendidas.' }); } },
    { name: 'welcomeoff', aliases: ['bienvenidaoff'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '👋 Bienvenidas apagadas.' }); } },
    { name: 'byeon', aliases: ['despedidaon'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🚪 Despedidas encendidas.' }); } },
    { name: 'byeoff', aliases: ['despedidaoff'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🚪 Despedidas apagadas.' }); } },
    { name: 'infousuario', aliases: ['userinfo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: 'ℹ️ Información del miembro.' }); } },
    { name: 'activos', aliases: ['listactivos'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '👥 Listando miembros activos.' }); } },
    { name: 'gprules', aliases: ['reglas'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📜 Reglas del grupo establecidas.' }); } }
];
