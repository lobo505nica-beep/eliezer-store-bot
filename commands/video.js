module.exports = [
    { name: 'tovideo', aliases: ['mp4maker'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎬 Convirtiendo archivo a video...' }); } },
    { name: 'gifvideo', aliases: ['gvideo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎞️ GIF convertido a video MP4...' }); } },
    { name: 'fastvideo', aliases: ['fastmp4'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '⏩ Acelerando velocidad de video...' }); } },
    { name: 'slowvideo', aliases: ['slowmp4'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '⏪ Cámara lenta aplicada a video...' }); } },
    { name: 'reversetv', aliases: ['reversa'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔄 Video reproducido en reversa...' }); } },
    { name: 'cutvideo', aliases: ['cortarvideo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✂️ Cortando fragmento de video...' }); } },
    { name: 'joinvideo', aliases: ['unirvideo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔗 Uniendo clips de video...' }); } },
    { name: 'addaudio', aliases: ['ponerleaudio'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎵 Audio incrustado en el video...' }); } },
    { name: 'removeaudio', aliases: ['silenciarvideo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔇 Audio removido del video...' }); } },
    { name: 'rotatevideo', aliases: ['girarvideo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔄 Video girado 90/180 grados...' }); } },
    { name: 'resizevideo', aliases: ['cambiarsize'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📐 Redimensionando resolución de video...' }); } },
    { name: 'compressvideo', aliases: ['comprimirvideo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📦 Comprimiendo peso de video...' }); } },
    { name: 'watermarkvideo', aliases: ['marcadeagua'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✏️ Añadiendo marca de agua a video...' }); } },
    { name: 'textonvideo', aliases: ['tvideo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '💬 Incrustando subtítulos en video...' }); } },
    { name: 'videoflip', aliases: ['espejovideo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🪞 Efecto espejo en video...' }); } },
    { name: 'videobw', aliases: ['grisvideo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '⬛ Video en blanco y negro...' }); } },
    { name: 'videosepia', aliases: ['vsepia'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📜 Filtro sepia en video...' }); } },
    { name: 'videonegative', aliases: ['vnegativo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔄 Video en negativo...' }); } },
    { name: 'videofade', aliases: ['vfade'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✨ Efecto desvanecido en video...' }); } },
    { name: 'videoloop', aliases: ['vloop'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔁 Video en bucle infinito...' }); } },
    { name: 'videocrop', aliases: ['recortarv'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📐 Recortando marco de video...' }); } },
    { name: 'videoenhance', aliases: ['mejorarv'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✨ Mejorando calidad de video...' }); } },
    { name: 'videostabilize', aliases: ['estabilizarv'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎥 Estabilizando movimiento de cámara...' }); } },
    { name: 'videofps', aliases: ['fpsvideo'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '⚡ Ajustando cuadros por segundo...' }); } },
    { name: 'videobitrate', aliases: ['vbitrate'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📊 Ajustando bitrate de video...' }); } },
    { name: 'videowebm', aliases: ['webm'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🌐 Convertido a formato WebM...' }); } },
    { name: 'videomov', aliases: ['mov'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🍎 Convertido a formato MOV...' }); } },
    { name: 'videoavi', aliases: ['avi'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '💻 Convertido a formato AVI...' }); } },
    { name: 'videomkv', aliases: ['mkv'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎬 Convertido a formato MKV...' }); } },
    { name: 'videoflv', aliases: ['flv'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📦 Convertido a formato FLV...' }); } },
    { name: 'videocircle', aliases: ['vcircle'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '⭕ Video redondo para nota de video...' }); } },
    { name: 'videocollage', aliases: ['vcollage'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🖼️ Collage de múltiples videos...' }); } },
    { name: 'videoeffect', aliases: ['vfx'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✨ Efecto visual aplicado a video...' }); } },
    { name: 'videocolor', aliases: ['vcolor'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎨 Corrección de color en video...' }); } },
    { name: 'videovignette', aliases: ['vvignette'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🖼️ Viñeta aplicada a video...' }); } },
    { name: 'videoblur', aliases: ['vblur'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🌫️ Desenfoque aplicado a video...' }); } },
    { name: 'videosharpen', aliases: ['vsharpen'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✨ Nitidez aumentada en video...' }); } },
    { name: 'videodenoise', aliases: ['vdenoise'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🧹 Reducción de ruido en video...' }); } },
    { name: 'videoroom', aliases: ['vzoom'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔍 Zoom aplicado a video...' }); } },
    { name: 'videopanning', aliases: ['vpanning'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🎥 Panning aplicado a video...' }); } },
    { name: 'videorecord', aliases: ['vrecord'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔴 Grabación de video procesada...' }); } },
    { name: 'videosplit', aliases: ['vsplit'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '✂️ Video dividido en partes...' }); } },
    { name: 'videomerge', aliases: ['vmerge'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔗 Videos fusionados...' }); } },
    { name: 'videorender', aliases: ['vrender'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '⚙️ Renderizando video final...' }); } },
    { name: 'videostats', aliases: ['vstats'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📊 Estadísticas de renderizado...' }); } },
    { name: 'videohelp', aliases: ['vhelp'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '📖 Guía de creación de video.' }); } },
    { name: 'videoclear', aliases: ['vclear'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🧹 Caché de video limpia.' }); } },
    { name: 'videolock', aliases: ['vlock'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔒 Módulo de video bloqueado.' }); } },
    { name: 'videounlock', aliases: ['vunlock'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🔓 Módulo de video desbloqueado.' }); } },
    { name: 'videostatus', aliases: ['vstatus'], execute: async (sock, m, args, r) => { await sock.sendMessage(r, { text: '🟢 Módulo de video activo.' }); } }
];
